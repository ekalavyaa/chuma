var util = require('./util.functions');

module.exports = (req, field, body, name) => {
    switch(name) {
        case 'not_empty': util.
        break;
        case 'is_int': console.log('is_int called');
        break;
    }
}